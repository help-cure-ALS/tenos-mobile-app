import type { UITokens } from 'react-native-nice-ui';

export interface AppTokens extends UITokens {
    // App-spezifische Tokens
    cardRadius: number;
    cardPadding: number;
    screenPaddingHorizontal: number;
}
